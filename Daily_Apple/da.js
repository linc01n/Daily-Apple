// Generated by CoffeeScript 1.4.0
(function() {
  var AppleDaily, Article, root,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Article = (function() {

    function Article() {}

    Article.prototype.set_title = function(title) {
      this.title = title;
      return this.title = $.trim(this.title);
    };

    Article.prototype.set_content = function(content) {
      this.content = content;
      return this.content = $.trim(this.content);
    };

    Article.prototype.set_date = function(date) {
      this.date = date;
      return this.date = $.trim(this.date);
    };

    Article.prototype.set_section = function(section) {
      this.section = section;
      return this.section = $.trim(this.section);
    };

    Article.prototype.set_view_count = function(view_count) {
      this.view_count = view_count;
      return this.view_count = $.trim(this.view_count);
    };

    return Article;

  })();

  AppleDaily = (function(_super) {

    __extends(AppleDaily, _super);

    function AppleDaily() {
      return AppleDaily.__super__.constructor.apply(this, arguments);
    }

    AppleDaily.prototype.parse = function(html) {
      var page, raw_content;
      page = $(html);
      this.set_title(page.find("#articleContent h1").text());
      this.set_view_count(page.find("#articleContent .view").text());
      raw_content = page.find("#masterContent .ArticleContent_Inner");
      this.set_content(_.chain(raw_content).map(function(paragraph) {
        return $(paragraph).children();
      }).map(function(node) {
        return $(node).contents().filter(function() {
          return this.nodeType === 8 || this.nodeName === "IMG";
        });
      }).map(function(child) {
        return $(child).removeAttr("class").parent().html(function(idx, html) {
          return html.replace(/^\s+|\s+$/g, "").replace(/>\s+</g, "><");
        }).html();
      }).reduce(function(memo, obj) {
        return memo + obj;
      }).value());
      return this.set_date(page.find(".LHSTitle_inner .SelectHdate").text());
    };

    return AppleDaily;

  })(Article);

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.Article = Article;

  root.AppleDaily = AppleDaily;

}).call(this);
